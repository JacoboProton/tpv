{
  "name": "tpv-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "tpv-restaturante"
  ],
  "scripts": {
    "prepare": "husky",
    "build:core": "npm run build --workspace @tpv/core",
    "build": "npm run build --workspace tpv-restaturante",
    "test": "npm run test --workspaces --if-present"
  }
}