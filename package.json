{
  "name": "tpv-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "tpv-restaturante"
  ],
  "scripts": {
    "prepare": "husky",
    "build:core": "npm run build --workspace @tpv/core",
    "build": "npm run build --workspace tpv-restaurante",
    "test": "npm run test --workspaces --if-present"
  }
}